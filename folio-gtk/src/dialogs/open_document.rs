use libadwaita::{Application, ApplicationWindow};

/// Show the file-open dialog.
pub fn show(_app: &Application, _parent: &ApplicationWindow) {
    // TODO Phase 3: gtk::FileDialog::new().open() async.
}
